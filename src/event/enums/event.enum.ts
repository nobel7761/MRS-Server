export enum EventStatus {
  UPCOMING = 'Upcoming',
  ONGOING = 'Ongoing',
  COMPLETED = 'Completed',
}

export enum EventVisibility {
  PUBLIC = 'Public',
  PRIVATE = 'Private',
  ALUMNI_ONLY = 'Alumni-only',
}

// Removed predefined pricing ranges - users will set their own batch ranges
